# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

               |-----handy-ruler------------------------------------------------------|
arm-x-toolchain: arm-x-toolchain (gcc and g++ cross compiler for Slackware ARM)
arm-x-toolchain:
arm-x-toolchain: This is Stuart Winter's x-toolchain from the Slackware ARM devtools.
arm-x-toolchain: It is intended to be installed on a distcc server, and called from a
arm-x-toolchain: Slackware ARM client using masquerade mode.  The distcc server should
arm-x-toolchain: be running either Slackware64 (x86_64) or Slackware (i486).
arm-x-toolchain:
arm-x-toolchain: https://github.com/idlemoor/distcc-tools
arm-x-toolchain:
arm-x-toolchain:
arm-x-toolchain:
