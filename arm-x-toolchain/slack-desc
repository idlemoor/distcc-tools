# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

               |-----handy-ruler------------------------------------------------------|
arm-x-toolchain: arm-x-toolchain (gcc and g++ cross compiler for Slackware ARM)
arm-x-toolchain:
arm-x-toolchain: This SlackBuild packages Stuart Winter's x-toolchain (from the
arm-x-toolchain: Slackware ARM devtools).  It is intended to be installed on a distcc
arm-x-toolchain: server, and called from a Slackware ARM client using masquerade mode.
arm-x-toolchain:
arm-x-toolchain: https://github.com/idlemoor/distcc-tools
arm-x-toolchain:
arm-x-toolchain:
arm-x-toolchain:
arm-x-toolchain:
